Claude 4 / 4.5 / 4.6 Model Upgrade - Rollback Instructions
===========================================================
Use these instructions to roll back to a stable 4-model configuration.

Rollback target models:
  - us.anthropic.claude-sonnet-4-20250514-v1:0    (Claude Sonnet 4) [DEFAULT]
  - us.anthropic.claude-3-7-sonnet-20250219-v1:0  (Claude 3.7 Sonnet)
  - us.meta.llama4-scout-17b-instruct-v1:0        (Llama 4 Scout)
  - us.meta.llama4-maverick-17b-instruct-v1:0     (Llama 4 Maverick)

Models being removed:
  - us.anthropic.claude-haiku-4-5-20251001-v1:0   (Claude Haiku 4.5)
  - us.anthropic.claude-sonnet-4-5-20250929-v1:0  (Claude Sonnet 4.5)
  - us.anthropic.claude-opus-4-5-20251101-v1:0    (Claude Opus 4.5)
  - us.anthropic.claude-opus-4-6-v1               (Claude Opus 4.6)

Code rollback target:
  - Tag: release-0 (servicetrade branch @ 3c0ed94f3)
  - litellm 1.79.0, openai 2.6.1

Rollback Steps
--------------

1. Switch back to the release-0 tag

   cd /root/danswer
   git fetch && git checkout release-0

2. Rebuild the Docker image with the old code

   docker build -t onyxdotapp/onyx-backend:latest -f backend/Dockerfile backend/

3. Restart the stack

   cd /root/danswer/deployment/docker_compose
   docker-compose -f docker-compose.prod-no-letsencrypt.yml -p danswer-stack down
   docker-compose -f docker-compose.prod-no-letsencrypt.yml -p danswer-stack up -d

4. Wait for containers and Vespa

   docker ps --format "table {{.Names}}\t{{.Status}}"

   Vespa will need 20-30+ minutes to reload its index. Monitor:
   docker exec danswer-stack-index-1 curl -s http://localhost:8081/state/v1/health | grep code

5. Restore the model configuration in the database

   # Check current state
   docker exec -it danswer-stack-relational_db-1 psql -U postgres -d postgres \
     -c "SELECT id, name FROM llm_provider;"

   docker exec -it danswer-stack-relational_db-1 psql -U postgres -d postgres \
     -c "SELECT * FROM model_configuration;"

   # Clear all model configurations
   docker exec -it danswer-stack-relational_db-1 psql -U postgres -d postgres \
     -c "DELETE FROM model_configuration;"

   # Insert the 4 rollback models (adjust llm_provider_id if not 3)
   docker exec -it danswer-stack-relational_db-1 psql -U postgres -d postgres \
     -c "INSERT INTO model_configuration (name, is_visible, llm_provider_id, max_input_tokens) VALUES ('us.anthropic.claude-sonnet-4-20250514-v1:0', true, 3, 200000), ('us.anthropic.claude-3-7-sonnet-20250219-v1:0', true, 3, 200000), ('us.meta.llama4-scout-17b-instruct-v1:0', true, 3, NULL), ('us.meta.llama4-maverick-17b-instruct-v1:0', true, 3, NULL);"

   # Set Claude Sonnet 4 as default
   docker exec -it danswer-stack-relational_db-1 psql -U postgres -d postgres \
     -c "UPDATE llm_provider SET default_model_name = 'us.anthropic.claude-sonnet-4-20250514-v1:0', fast_default_model_name = 'us.anthropic.claude-sonnet-4-20250514-v1:0' WHERE name = 'AWS Bedrock';"

6. Verify

   - Log in at https://kevin-staging.servicetrade.com
   - Model dropdown should show exactly 4 models:
     * Claude Sonnet 4
     * Claude 3.7 Sonnet
     * Llama 4 Scout
     * Llama 4 Maverick
   - Default should be Claude Sonnet 4
   - Send a test message to confirm chat works
   - Try a search query to confirm RAG works

Notes
-----
- The full stack restart means another 20-30+ minute Vespa loading wait.
  Plan accordingly.
- If you only need to rollback the models (not the code), you can skip
  steps 1-3 and just run the SQL in step 5, then restart only api_server:
  docker-compose -f docker-compose.prod-no-letsencrypt.yml -p danswer-stack restart api_server
- The feb-new-models branch and PR #4 will still exist for re-deployment later.
