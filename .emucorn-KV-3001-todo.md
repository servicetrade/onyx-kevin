# Implementation Plan: KV-3001 - Upgrade LLM Models to Claude 4.5 Family

**Story Type**: Full-stack (backend dependencies + config, frontend display names, EE provisioning)
**Estimated Tasks**: 4 automatable tasks (consolidated dependency changes into one task)
**Approach**: Targeted dependency upgrades in requirements files (openai v2, remove langchain-openai, swap llama-index for llama-index-core), then configuration updates for frontend model display names and EE tenant provisioning defaults. No logic changes, no new tests (scout confirmed no test patterns exist for any files being modified).

## Key Decisions
- **Bedrock defaults**: Keep both `default_model` and `default_fast_model` as Haiku 4.5. Already correct in current code. No changes needed.
- **Old models in `defaultModelsByProvider`**: Keep old models alongside new Claude 4.5 entries. Add new, don't remove old.
- **Task 5 (llm_provider_options.py)**: Already done correctly on branch. Both Bedrock defaults are Haiku 4.5, Anthropic visible models are all three Claude 4.5 models. No further changes needed.
- **No new unit tests**: Scout confirmed none of the files being changed (requirements, hooks.ts, provisioning.py) have established test patterns. Docker build success is the key validation.
- **Tasks 8-10 (deploy, DB config, verification)**: Excluded from plan -- user will handle manually.

## Pre-existing Work (Already Done)
- Task 1: litellm bumped to 1.81.3 in both `default.txt` and `model_server.txt`
- Task 5: `llm_provider_options.py` fully updated (BEDROCK_DEFAULT_MODEL = Haiku 4.5, visible models = all three Claude 4.5 models, Anthropic defaults = Sonnet 4.5 / Haiku 4.5)

## Task Breakdown

### Validate Existing Work

- [x] Review and validate existing changes on feb-new-models branch (litellm bump + llm_provider_options.py) [Difficulty: 3/10] ✅ All correct, no fixes needed
  Files: `backend/requirements/default.txt`, `backend/requirements/model_server.txt`, `backend/onyx/llm/llm_provider_options.py`
  Notes: Validate that the existing unstaged changes are correct and complete:
  1. `default.txt`: Verify litellm==1.81.3 is set correctly
  2. `model_server.txt`: Verify litellm==1.81.3 is set correctly
  3. `llm_provider_options.py`: Verify ALL changes are correct:
     - BEDROCK_DEFAULT_MODEL = `us.anthropic.claude-haiku-4-5-20251001-v1:0`
     - ANTHROPIC_VISIBLE_MODEL_NAMES = all three Claude 4.5 models (haiku, sonnet, opus)
     - Bedrock visible models list = all three `us.anthropic.*` models
     - Anthropic provider defaults: default_model=Sonnet 4.5, default_fast_model=Haiku 4.5
     - Bedrock provider defaults: BOTH default_model AND default_fast_model = BEDROCK_DEFAULT_MODEL (Haiku 4.5)
  4. Check for any issues, typos, or missing changes. Fix anything that's wrong.
  Citation: Story KV-3001.md Tasks 1 and 5; git diff shows current unstaged changes

### Dependencies (Requirements Files)

- [x] Update openai to v2, remove langchain-openai, and swap llama-index for llama-index-core [Difficulty: 3/10] ✅
  Files: `backend/requirements/default.txt` (3 edits) and `backend/requirements/model_server.txt` (1 edit)
  Notes:
  1. `default.txt` line 51: `openai==1.75.0` -> `openai==2.8.0`
  2. `default.txt` line 36: Delete `langchain-openai==0.2.9` entirely (never imported anywhere in codebase, confirmed by scout). **Fallback**: if pip resolution fails, replace with `langchain-openai==0.3.35` instead of deleting.
  3. `default.txt` line 45: `llama-index==0.12.28` -> `llama-index-core==0.12.28` (only `SentenceSplitter` from `llama_index.core.node_parser` is used, at `/backend/onyx/indexing/chunker.py:139`, import path identical in llama-index-core)
  4. `model_server.txt` line 7: `openai==1.75.0` -> `openai==2.8.0` (must stay in sync with default.txt)
  Citation: `/Users/ericburns/src/onyx-kevin/backend/requirements/default.txt` lines 36, 45, 51; `/Users/ericburns/src/onyx-kevin/backend/requirements/model_server.txt` line 7

### Frontend (Display Names)

- [x] Add Claude 4.5 model display names and default models to `web/src/lib/hooks.ts` [Type: frontend] [Difficulty: 3/10] ✅
  Notes: Two changes in this file:
  1. Add 9 entries to `MODEL_DISPLAY_NAMES` dict (after the existing Anthropic section, around line 734). Three Anthropic direct IDs, three `us.anthropic.` Bedrock IDs, three `anthropic.` Bedrock IDs:
     - `"claude-haiku-4-5-20251001": "Claude Haiku 4.5"`
     - `"claude-sonnet-4-5-20250929": "Claude Sonnet 4.5"`
     - `"claude-opus-4-5-20251101": "Claude Opus 4.5"`
     - `"us.anthropic.claude-haiku-4-5-20251001-v1:0": "Claude Haiku 4.5"`
     - `"us.anthropic.claude-sonnet-4-5-20250929-v1:0": "Claude Sonnet 4.5"`
     - `"us.anthropic.claude-opus-4-5-20251101-v1:0": "Claude Opus 4.5"`
     - `"anthropic.claude-haiku-4-5-20251001-v1:0": "Claude Haiku 4.5"`
     - `"anthropic.claude-sonnet-4-5-20250929-v1:0": "Claude Sonnet 4.5"`
     - `"anthropic.claude-opus-4-5-20251101-v1:0": "Claude Opus 4.5"`
  2. Add Claude 4.5 entries to `defaultModelsByProvider` (keep existing old models per user decision):
     - `bedrock` array (line 831): Add both `us.anthropic.*` and `anthropic.*` variants for consistency (existing entries use `anthropic.*` prefix):
       `"us.anthropic.claude-haiku-4-5-20251001-v1:0"`, `"us.anthropic.claude-sonnet-4-5-20250929-v1:0"`, `"us.anthropic.claude-opus-4-5-20251101-v1:0"`,
       `"anthropic.claude-haiku-4-5-20251001-v1:0"`, `"anthropic.claude-sonnet-4-5-20250929-v1:0"`, `"anthropic.claude-opus-4-5-20251101-v1:0"`
     - `anthropic` array (line 839): Add `"claude-haiku-4-5-20251001"`, `"claude-sonnet-4-5-20250929"`, `"claude-opus-4-5-20251101"`
  Pattern: Follow existing entry format in `MODEL_DISPLAY_NAMES` (lines 718-734 for Anthropic, 773-806 for Bedrock). The `getDisplayNameForModel()` function (line 809) strips `bedrock/` prefix before lookup, so entries need the raw model ID without that prefix. Note: existing `defaultModelsByProvider.bedrock` entries use the `anthropic.*` prefix (not `us.anthropic.*`), so add both variants.
  Citation: `/Users/ericburns/src/onyx-kevin/web/src/lib/hooks.ts` lines 670-840

### Backend (EE Provisioning)

- [x] Update EE tenant provisioning default model names in `backend/ee/onyx/server/tenants/provisioning.py` [Difficulty: 2/10] ✅
  Notes: Two string replacements in the `configure_default_api_keys` function:
  - Line 273: `default_model_name="claude-3-7-sonnet-20250219"` -> `default_model_name="claude-sonnet-4-5-20250929"`
  - Line 274: `fast_default_model_name="claude-3-5-sonnet-20241022"` -> `fast_default_model_name="claude-haiku-4-5-20251001"`
  The `ANTHROPIC_VISIBLE_MODEL_NAMES` import (used on line 278) is already updated to Claude 4.5 models in `llm_provider_options.py`, so visibility is already correct.
  Citation: `/Users/ericburns/src/onyx-kevin/backend/ee/onyx/server/tenants/provisioning.py` lines 267-284

### Verification

- [x] Validate dependency resolution with pip dry-run [Difficulty: 3/10] ✅
  Notes: After all requirements changes are made, run:
  ```
  cd /Users/ericburns/src/onyx-kevin/backend && pip install --dry-run -r requirements/default.txt 2>&1 | tail -20
  ```
  **Success**: Command exits 0 with "Would install..." output showing openai-2.8.0 and llama-index-core-0.12.28.
  **Failure scenarios and remediation**:
  - If `langchain-openai` removal causes resolution error: Replace deleted line with `langchain-openai==0.3.35` (accepts `openai<3.0.0`).
  - If `llama-index-core==0.12.28` not found: Check PyPI for available versions, try `0.12.29` or latest `0.12.x`.
  - If openai v2 conflicts with another package: Check error output for the conflicting package and pin as needed.
  Also verify `model_server.txt`: `pip install --dry-run -r requirements/model_server.txt`

  **Validation Result**: ✅ PASSED
  - Exit code: 0
  - Confirmed "Would install" includes: openai-2.8.0, llama-index-core-0.12.28, litellm-1.81.3
  - No dependency conflicts detected
  - langchain-openai removal did NOT cause any issues (as predicted by research)
  - All packages exist on PyPI at specified versions

## Implementation Notes

**Patterns to Follow**:
- `MODEL_DISPLAY_NAMES` entries: Use the exact model ID as key, human-friendly name as value. Include all ID variants (direct, `us.anthropic.`, `anthropic.`).
- `defaultModelsByProvider`: Array of model ID strings per provider. Keep old entries, add new ones.
- EE provisioning: Uses Anthropic direct model IDs (not Bedrock IDs) since it provisions the Anthropic provider directly.

**Avoid**:
- Do NOT remove old models from `defaultModelsByProvider` (user decision: keep alongside new ones)
- Do NOT change `llm_provider_options.py` (Task 5 is already complete)
- Do NOT create tasks for deployment, DB config, or manual verification (Tasks 8-10 are user-handled)

**Dependency Change Summary**:
| File | Change | Reason |
|---|---|---|
| `default.txt` | `openai==1.75.0` -> `openai==2.8.0` | litellm 1.81.3 requires openai >= 2.8.0 |
| `default.txt` | Remove `langchain-openai==0.2.9` | Never imported, blocks openai v2 (`openai<2.0.0`) |
| `default.txt` | `llama-index==0.12.28` -> `llama-index-core==0.12.28` | Meta-package pulls `llama-index-llms-openai` which blocks openai v2 |
| `model_server.txt` | `openai==1.75.0` -> `openai==2.8.0` | Must stay in sync with default.txt |

## Success Criteria
- [ ] All acceptance criteria met (verified via manual deployment -- Tasks 8-10)
- [ ] No new test failures introduced (no test patterns exist for changed files)
- [ ] Dependency resolution succeeds (pip install / Docker build)
- [ ] No code duplication introduced
- [ ] Follows established patterns for model display names and provider defaults
